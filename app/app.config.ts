export default defineAppConfig({
  ui: {
    colors: {
      primary: 'orange',
      gray: 'cool',
    },

    prose: {
      img: {
        slots: {
          base: 'w-fit max-w-full mx-auto',
        },
      },
    },
  },
})
